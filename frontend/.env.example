# Argyle Payments Dashboard — environment template
# Copy to .env.development.local for personal overrides. Do not commit secrets.
# All variables MUST be prefixed VITE_ to be exposed to the client bundle.

VITE_API_BASE_URL=http://localhost:8080
VITE_DEV_BEARER_TOKEN=dev-key-mrc-acme
VITE_ENV_LABEL=dev
